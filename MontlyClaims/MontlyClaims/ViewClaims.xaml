<Window x:Class="MontlyClaims.ViewClaims"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MontlyClaims"
        mc:Ignorable="d"
        Title="Previous Claims - Lecturer"
        Height="700"
        Width="1000"
        WindowStartupLocation="CenterOwner"
        Background="#f8f9fa"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="#f39c12"
                CornerRadius="8"
                Padding="20"
                Margin="0,0,0,15">
            <TextBlock Text="Previous Claims History"
                       Foreground="White"
                       FontSize="22"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- Filters -->
        <Border Grid.Row="1"
                Background="White"
                CornerRadius="8"
                Padding="15"
                BorderThickness="1"
                BorderBrush="#e0e0e0"
                Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">

                <!-- Status Filter -->
                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Filter by Status"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="#2c3e50"
                               Margin="0,0,0,5"/>
                    <ComboBox x:Name="cmbStatusFilter"
                              Width="120"
                              Height="30"
                              FontSize="12"
                              Padding="5"
                              SelectionChanged="cmbStatusFilter_SelectionChanged">
                        <ComboBoxItem Content="All Status" IsSelected="True"/>
                        <ComboBoxItem Content="Submitted"/>
                        <ComboBoxItem Content="Approved"/>
                        <ComboBoxItem Content="Rejected"/>
                        <ComboBoxItem Content="Pending"/>
                        <ComboBoxItem Content="Paid"/>
                    </ComboBox>
                </StackPanel>

                <!-- Year Filter -->
                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Filter by Year"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="#2c3e50"
                               Margin="0,0,0,5"/>
                    <ComboBox x:Name="cmbYearFilter"
                              Width="100"
                              Height="30"
                              FontSize="12"
                              Padding="5"
                              SelectionChanged="cmbYearFilter_SelectionChanged">
                        <ComboBoxItem Content="All Years" IsSelected="True"/>
                    </ComboBox>
                </StackPanel>

                <!-- Search Box -->
                <StackPanel>
                    <TextBlock Text="Search Claims"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="#2c3e50"
                               Margin="0,0,0,5"/>
                    <TextBox x:Name="txtSearch"
                             Width="200"
                             Height="30"
                             FontSize="12"
                             Padding="5"
                             TextChanged="txtSearch_TextChanged"
                             Tag="Search by month or notes..."/>
                </StackPanel>

                <!-- Refresh Button -->
                <Button x:Name="btnRefresh"
                        Content="🔄 Refresh"
                        Width="100"
                        Height="30"
                        Background="#3498db"
                        Foreground="White"
                        FontSize="12"
                        FontWeight="SemiBold"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="btnRefresh_Click"
                        Margin="20,15,0,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="4"
                                    Padding="5">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#2980b9"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#1f6391"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Border>

        <!-- Example claims list -->
        <ListView Grid.Row="2" x:Name="lstClaims" Margin="0,0,0,15">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Month" DisplayMemberBinding="{Binding Month}" Width="200"/>
                    <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}" Width="150"/>
                    <GridViewColumn Header="Amount" DisplayMemberBinding="{Binding Amount}" Width="150"/>
                    <GridViewColumn Header="Notes" DisplayMemberBinding="{Binding Notes}" Width="400"/>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Footer -->
        <TextBlock Grid.Row="3" Text="© 2025 Monthly Claims System"
                   HorizontalAlignment="Center"
                   Foreground="#7f8c8d"
                   FontSize="12"/>
    </Grid>
</Window>
